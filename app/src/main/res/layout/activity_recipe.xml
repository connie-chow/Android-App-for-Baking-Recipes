<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- LinearLayout for holding two pane layout of recipe steps and step details -->
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/android_me_linear_layout"
    android:background="@android:color/white"
    android:tag="regular_layout"
    android:orientation="vertical">


        <!-- This container holds the Recipe Step List Fragment -->
        <FrameLayout android:id="@+id/recipe_container"
            android:layout_width="match_parent"
            android:background="@android:color/white"
            android:layout_height="wrap_content"
            android:scaleType="centerInside"/>

<!--
android:layout_height="500dp"
    <FrameLayout android:id="@+id/recipe_details_container"
        android:layout_width="match_parent"
        android:layout_height="180dp"
        android:scaleType="centerInside"/>
        -->

</LinearLayout>
</ScrollView>

<!--

    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">
-->